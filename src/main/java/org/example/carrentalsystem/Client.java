package org.example.carrentalsystem;

import java.util.Date;

public class Client extends User {
    public Client(int userId, String userName, String password , Role role) {
        super(userId, userName, password, role);
    }
}
